# HANDOFF 2026-04-17

Resumen de la sesión del 17 de abril para arrancar el siguiente chat sin perder contexto.

---

## Qué se hizo hoy

Se montó la infraestructura de documentación del proyecto:

- `CLAUDE.md` (raíz): punto de entrada para Claude Code.
- `AGENTS.md` (raíz): actualizado, contexto permanente del problema.
- `docs/STATE.md`: estado vivo del proyecto.
- `docs/EXPERIMENTS.md`: historial de las 15+ iteraciones.
- `docs/WORKFLOW.md`: operativa del repo (Colab, Git, Drive).

Se validó con Claude Code (Sonnet + Opus) que la arquitectura de documentación funciona.

---

## Estado actual

- Modelo en producción: `modelo_H1_sinSF` (v1). NSE=0.861, error pico -21.0% (infraestimación moderada).
- Problema sin resolver: infraestimación sistemática de picos extremos (~83%).
- Prioridad: mejorar el modelo. TFM después. Margen hasta septiembre 2026.

---

## Próximo paso recomendado

**Antes de iterar más sobre el modelo, hacer una revisión global con Opus 4.7 en Claude Code.**

Razón: es el primer proyecto de Deep Learning de Aimar. Llevamos 15+ iteraciones. Una revisión externa experta puede detectar errores metodológicos ocultos, confirmar si estamos cerca del límite físico del problema, o recomendar cambio de arquitectura.

### Preparación previa

Antes de lanzar la revisión, generar un resumen estadístico del dataset en `docs/DATASET_STATS.md` (distribuciones, correlaciones, caracterización de los 59 eventos extremos). Script Python de ~50 líneas.

### Prompt exacto para la revisión

Eres un revisor externo experto en Deep Learning aplicado a hidrología y series temporales.
Tu trabajo NO es validar mi proyecto, es encontrar lo que está mal o subóptimo.
Has cargado: CLAUDE.md, STATE.md, AGENTS.md, EXPERIMENTS.md, src/, notebooks/claude_train.py,
local_eval_metrics.json, docs/DATASET_STATS.md.
Es mi primer proyecto real de Deep Learning. Llevo 15+ iteraciones y el error en picos
extremos sigue siendo ~83% sistemático.
Respóndeme con BRUTAL honestidad a estas tres preguntas separadas:

DIAGNÓSTICO METODOLÓGICO: ¿hay algún error grave que se me haya pasado? (leakage temporal,
normalización mal aplicada, split mal hecho, features mal construidas, etc.). Solo lo que
está mal, con evidencia específica.
DIAGNÓSTICO DE LÍMITE FÍSICO: ¿es razonable pensar que el modelo está cerca del límite
superior con estos datos? ¿O crees que hay 10-15% más de NSE extraíble por alguien con
experiencia? Justifica con números.
RECOMENDACIÓN: si tuvieras que liderar este proyecto con 3 meses de margen (hasta julio
2026), ¿qué harías exactamente? Una decisión específica (arquitectura, loss, features,
evaluación), no cuatro opciones abiertas.

Reglas: no me adules, no respuestas políticamente correctas. Si algo está mal, dilo claro.

---

## Decisión pendiente (secundaria a la revisión)

Antes de lanzar la revisión, Opus en Claude Code recomendó **Opción B** (multi-cabeza con clasificación de 5 niveles) como siguiente iteración. El matiz que salió del chat del proyecto:

Antes de implementar B entera, hacer un **experimento de bajo coste** (1-2 horas, sin reentrenar): post-procesar las predicciones del modelo actual aplicando umbrales de clase y ver la matriz de confusión sobre los 59 extremos. Esto valida si la hipótesis de B tiene base.

Pero este experimento puede cambiar según lo que salga de la revisión con Opus, así que conviene hacer la revisión PRIMERO.

---

## Primer mensaje para el siguiente chat

> "Hola. Venimos del chat del 17 de abril donde consolidamos la documentación del proyecto. 
> Lee `docs/HANDOFF_2026-04-17.md` y dime el siguiente paso concreto."