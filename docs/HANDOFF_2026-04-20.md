# HANDOFF 2026-04-20

Resumen de la sesión del 20-abril para arrancar el siguiente chat sin perder contexto.

---

## Qué se hizo hoy

Sesión larga con tres bloques de trabajo:

1. **Corrección de documentación existente.** Se detectó una inconsistencia entre las cifras de `STATE.md` y `local_eval_metrics.json` para el modelo H1_sinSF. Afectaba solo a las filas SIN SF. Corregidas NSE=0.861 (antes 0.819), peak_err=-21% (antes +12.8%), bias_ext=-12.7 (antes -3.7), y todo el bloque "Diagnóstico por rangos de magnitud".

2. **Generación de `docs/DATASET_STATS.md`.** Script nuevo `scripts/generate_dataset_stats.py` que reutiliza el pipeline de `src/` para generar 9 secciones: resumen global, distribución del target, eventos extremos, correlaciones feature-target, matriz feature-feature, ACF del target, caracterización de la lluvia, comparación con predictor naive (persistencia), y síntesis de hallazgos.

3. **Revisión externa con Opus 4.7.** Input: todo el paquete documental completo. Output: diagnóstico con 6 hallazgos concretos, cota teórica de NSE alcanzable (0.88-0.92), recomendación de 3 cambios coordinados, y 3 preguntas críticas no formuladas previamente. Archivada en `docs/OPUS_REVIEW_2026-04-20.md`.

---

## Estado actual

- **Modelo en producción:** `modelo_H1_sinSF` (v1). NSE=0.861, error pico -21.0% sobre test.
- **Hallazgo crítico que reorienta el proyecto:** el modelo bate al naive por solo +0.050 NSE a H=1, y es PEOR que naive a H≥3. La pregunta central del proyecto es si esa ganancia viene de las features de lluvia o de `delta_flow_5m`/`delta_flow_15m`, que son puerta trasera al atajo de `flow_total_mgd`.
- **Plan de iteraciones 16-20:** secuencial, cada una responde una pregunta concreta. Ver `STATE.md` sección "Siguientes pasos priorizados".

---

## Próximo paso concreto

Iteración 16: eliminar `delta_flow_5m` y `delta_flow_15m` de `FEATURE_COLUMNS` en `claude_train.py`. Reentrenar. Evaluar.

Este es el ablation crítico que responde a la pregunta D1 de Opus: ¿la ganancia sobre naive viene de lluvia o de los deltas de flow? El resultado decide si el proyecto sigue el plan 16-20 o tiene que rediseñarse desde cero.

---

## Primer mensaje para el siguiente chat

> "Hola. Venimos del chat del 20-abril donde revisamos el proyecto con Opus 4.7 y generamos el plan de iteraciones 16-20. Lee `docs/HANDOFF_2026-04-20.md` y ayúdame a preparar la iteración 16 (ablation de delta_flow_*)."